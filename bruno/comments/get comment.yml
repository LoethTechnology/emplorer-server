info:
  name: Get Comment
  type: http
  seq: 3

http:
  method: GET
  url: '{{local}}/reviews/{{reviewId}}/comments/{{commentId}}'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
