info:
  name: List Comments
  type: http
  seq: 2

http:
  method: GET
  url: '{{local}}/reviews/{{reviewId}}/comments?page=1&limit=10&sort=desc'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
