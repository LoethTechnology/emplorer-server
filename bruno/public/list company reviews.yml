info:
  name: List Company Reviews
  type: http
  seq: 2

http:
  method: GET
  url: '{{local}}/public/companies/{{companyId}}/reviews?page=1&limit=10&sort=desc'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
