info:
  name: Delete Critique
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{local}}/reviews/{{reviewId}}/critiques/{{critiqueId}}'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
