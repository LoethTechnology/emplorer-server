info:
  name: Delete Location
  type: http
  seq: 4

http:
  method: DELETE
  url: '{{local}}/companies/:companyId/locations/:locationId'
  params:
    - name: locationId
      value: '{{locationId}}'
      type: path
    - name: companyId
      value: '{{companyId}}'
      type: path
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
