info:
  name: Delete Location
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{local}}/companies/{{companyId}}/locations/{{locationId}}'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
