info:
  name: Delete Review
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{local}}/companies/{{companyId}}/reviews/{{reviewId}}'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
