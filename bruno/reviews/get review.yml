info:
  name: Get Review
  type: http
  seq: 3

http:
  method: GET
  url: '{{local}}/companies/{{companyId}}/reviews/{{reviewId}}'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
