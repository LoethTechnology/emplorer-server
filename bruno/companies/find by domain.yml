info:
  name: Find By Domain
  type: http
  seq: 4

http:
  method: GET
  url: '{{local}}/companies/domain/acme.com'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
