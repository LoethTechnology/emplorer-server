info:
  name: Get My Review
  type: http
  seq: 8

http:
  method: GET
  url: '{{local}}/user/me/reviews/{{reviewId}}'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
