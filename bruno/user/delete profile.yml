info:
  name: Delete Profile
  type: http
  seq: 5

http:
  method: DELETE
  url: '{{local}}/user/me'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
