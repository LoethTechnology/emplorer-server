info:
  name: Forgot Password
  type: http
  seq: 2

http:
  method: POST
  url: '{{local}}/auth/forgot-password'
  body:
    type: json
    data: |-
      {
        "email": "test@test.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
