info:
  name: LinkedIn Callback
  type: http
  seq: 7

http:
  method: GET
  url: '{{local}}/auth/linkedin/callback'
  body:
    type: json
    data: ''
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
